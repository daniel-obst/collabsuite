---
service_quay_io_init_config: "{{ ansible_deploy_production
  | ansible.builtin.ternary(platform_service_quayio.production, platform_service_quayio.staging) }}"

service_quay_io_init_username: "{{ service_quay_io_init_config.service_account.username }}"
service_quay_io_init_fqdn_service: "{{ service_quay_io_init_config.fqdn_service }}"
service_quay_io_init_fqdn_server: "{{ service_quay_io_init_config.fqdn_server }}"
