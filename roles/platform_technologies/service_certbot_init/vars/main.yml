---
service_certbot_init_config: "{{ ansible_deploy_production
  | ansible.builtin.ternary(platform_service_certbot.production, platform_service_certbot.staging) }}"

service_certbot_init_username: "{{ service_certbot_init_config.service_account.username }}"
service_certbot_init_process_service_cert: false
