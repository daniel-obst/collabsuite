---
service_it_tools_init_config: "{{ ansible_deploy_production
  | ansible.builtin.ternary(platform_service_it_tools.production, platform_service_it_tools.staging) }}"

service_it_tools_init_username: "{{ service_it_tools_init_config.service_account.username }}"
service_it_tools_init_fqdn_service: "{{ service_it_tools_init_config.fqdn_service }}"
service_it_tools_init_fqdn_server: "{{ service_it_tools_init_config.fqdn_server }}"
