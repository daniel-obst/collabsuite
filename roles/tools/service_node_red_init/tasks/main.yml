---
- name: Service (node-red) - init
  when: facts_machine_linux_distro
  block:
    - name: Service (node-red) - container service init
      ansible.builtin.include_role:
        name: oci_container_service_init
      vars:
        oci_container_service_init_username: "{{ service_node_red_init_username }}"
        oci_container_service_init_fqdn_service: "{{ service_node_red_init_fqdn_service }}"
        oci_container_service_init_fqdn_server: "{{ service_node_red_init_fqdn_server }}"
